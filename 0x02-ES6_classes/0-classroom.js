#!/usr/bin/node
export default class ClassRoom {
    constructor (maxStudentsSize) {
        this._maxStudent = maxStudentsSize;
    }
}

// const abbey = new ClassRoom(10);
// console.log(abbey._maxStudent);